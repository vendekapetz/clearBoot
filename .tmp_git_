echo "# clearBoot" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/vendekapetz/clearBoot.git
git push -u origin master

!!!!!!! fast of all you need ->
git init
git config user.email "vendekapetz@gmail.com"
git config user.name "vendekapetz"
git remote add origin https://github.com/vendekapetz/clearBoot.git

git branch steps
git checkout steps

!!!!!!! every time you must do this ->  GIT ADD . //

git add .
git commit -m "... message"
git push -u origin [branch] 

!!!!!!! merge ->

git checkout master
git merge steps
===================